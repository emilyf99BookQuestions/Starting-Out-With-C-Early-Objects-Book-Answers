#pragma once
#include <string>

using namespace std;
//Question Eight

class StringCompare {
public:
	static void StringCompareStart();
	static string UpperCaseIt(string s);
	static bool SameString(string s1, string s2);
};